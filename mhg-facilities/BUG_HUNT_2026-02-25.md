# Bug Hunt Checklist (2026-02-25)

## Scope
- Frontend and backend wiring consistency
- API contract correctness
- Schema/type consistency

## Issues
- [x] Fix users deactivate/reactivate flow (`PATCH /api/users/:id` + client hooks)
- [x] Fix users filter wiring (`search` / `status` params in `/api/users`)
- [x] Fix reports CSV export payload shape (`/reports` page -> `/api/reports/export`)
- [x] Fix vendor rating submission using placeholder ticket ID
- [x] Fix compliance status filter handling for "All Statuses"
- [x] Implement missing endpoint: `GET /api/assets/stats`
- [x] Implement missing endpoint: `GET /api/vendors/stats`
- [x] Implement missing endpoint: `GET /api/asset-categories/tree`
- [x] Support compliance stats response on `GET /api/compliance?stats=true`
- [x] Sync `locations.brand` in `src/types/database.ts` with migrations

## Notes
- Type-check currently passes.
- Unit tests currently have broad existing failures outside this bug list.
